# 污水厂运维管理系统 —— 依赖清单
# 版本放宽，兼容 Streamlit Cloud 的 Python 3.9-3.12 环境；
# 同时避免 Python 3.13 下某些旧版本无预编译 wheel 导致部署失败。
streamlit>=1.30.0
pandas>=2.0.0
numpy>=1.24.0
matplotlib>=3.7.0
plotly>=5.15.0
scipy>=1.10.0
openai>=1.0.0
python-dotenv>=1.0.0
reportlab>=4.0.0
